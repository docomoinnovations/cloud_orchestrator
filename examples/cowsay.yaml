kind: Pod
metadata:
  name: whalesay-pod
  inputs:
    parameters:
    - name: message
  container:
spec:
  containers:
    image: docker/whalesay
    command: [cowsay]
    args: ["{{inputs.parameters.message}}"]
