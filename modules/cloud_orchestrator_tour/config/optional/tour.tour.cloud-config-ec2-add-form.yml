uuid: f7f470f8-7e26-4db3-9c91-a66091742766
langcode: en
status: true
dependencies:
  module:
    - cloud_orchestrator_tour
id: cloud-config-ec2-add-form
label: cloud_config_ec2_add_form
module: cloud_orchestrator_tour
routes:
  -
    route_name: entity.cloud_config.add_form
    route_params:
      cloud_config_type: aws_ec2
tips:
  introduction:
    id: introduction
    plugin: text
    label: 'Add EC2 Provider'
    weight: -100
    attributes: null
    body: 'Use this page to add an AWS EC2 region.'
    location: top
  aws-profile:
    id: aws-profile
    plugin: text
    label: 'AWS Profile'
    weight: -100
    attributes:
      data-id: edit-profile
    body: 'Enter AWS credentials in this section.'
    location: right
  account:
    id: account
    plugin: text
    label: Account
    weight: -100
    attributes:
      data-id: edit-field-account-id-wrapper
    body: 'Enter AWS Account ID here.'
    location: right
  region:
    id: region
    plugin: text
    label: Region
    weight: -100
    attributes:
      data-id: edit-regions--wrapper
    body: 'Select regions to be managed.  Not all regions needs to be selected.'
    location: right
  credentials:
    id: credentials
    plugin: text
    label: Credentials
    weight: -100
    attributes:
      data-id: edit-credentials
    body: 'Enter  EC2 credentials in this section.  You can retrieve your credentials from the AWS console.'
    location: top
  assume-role:
    id: assume-role
    plugin: text
    label: 'Assume role'
    weight: -100
    attributes:
      data-id: edit-field-assume-role-wrapper
    body: "Use this section for enhanced security.  \r\n<p>\r\nThis is useful when the account needs to access another EC2 account through IAM assume role.\r\n</p>"
    location: right
  finish:
    id: finish
    plugin: text
    label: Finish
    weight: -100
    attributes: null
    body: "Save the form and the system will setup all the EC2 regions.\r\n<p>\r\nIf you have EC2 instances, they will automatically be imported.\r\n</p>"
    location: top
